<form method="post" action="{{ path('app_veicoli_delete', {'idVeicolo': veicoli.idVeicolo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veicoli.idVeicolo) }}">
    <button class="btn">Delete</button>
</form>
