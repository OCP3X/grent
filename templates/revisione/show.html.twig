{% extends 'base.html.twig' %}

{% block title %}Revisione{% endblock %}

{% block body %}
    <h1>Revisione</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ revisione.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_revisione_index') }}">back to list</a>

    <a href="{{ path('app_revisione_edit', {'id': revisione.id}) }}">edit</a>

    {{ include('revisione/_delete_form.html.twig') }}
{% endblock %}
