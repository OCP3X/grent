{% extends 'base.html.twig' %}

{% block title %}Documenti{% endblock %}

{% block body %}
    <h1>Documenti</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ documenti.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_documenti_index') }}">back to list</a>

    <a href="{{ path('app_documenti_edit', {'id': documenti.id}) }}">edit</a>

    {{ include('documenti/_delete_form.html.twig') }}
{% endblock %}
